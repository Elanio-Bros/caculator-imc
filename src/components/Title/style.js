// Arquivo de Estilisação feito pelo o react native

// Importe padrão do stylesheet do react-native
import { StyleSheet } from "react-native";

// Meio de Estilização do React Native
const styles = StyleSheet.create({
  boxTitle: {
    alignItems: "center",
    justifyContent: "center",
    padding: 10,
  },

  textTitle: {
    color: "#57C5B6",
    fontSize: 30,
    fontWeight: "bold",
  },
});

export default styles; // exportar a variavel styles para o padrão
